import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://maverickspectacle.github.io', // Your full deployed URL
  base: '/promethean-dissident',            // Your repository name (starts with a /)
});
